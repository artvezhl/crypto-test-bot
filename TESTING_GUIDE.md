# 🧪 Руководство по тестированию Спринта 4

> Пошаговая инструкция для тестирования всех компонентов бэктестинга

---

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Пересоздайте контейнер (если еще не сделали)

В VS Code:
```
Ctrl+Shift+P (или Cmd+Shift+P на Mac)
→ "Dev Containers: Rebuild Container"
```

Это установит Flask и настроит всё необходимое.

### Шаг 2: Запустите комплексный тест

В терминале контейнера:

```bash
cd /app
python test_all_sprint4.py
```

Этот скрипт:
- ✅ Проверит все зависимости
- ✅ Проверит конфигурацию (.env)
- ✅ Запустит все 4 теста последовательно
- ✅ Покажет итоговый отчет

**Ожидаемое время:** ~5-10 минут

---

## 📋 Детальное тестирование

### Тест 1: Базовый бэктест ⏱️ ~1-2 минуты

**Что проверяет:**
- Работа BacktestEngine
- Загрузка исторических данных
- Симуляция торговли
- Базовые метрики

**Запуск:**
```bash
python src/test_backtest.py
```

**Что должно быть:**
```
🚀 ЗАПУСК БЭКТЕСТА
📅 Период: 2025-12-XX - 2025-12-XX
📊 Символы: BTCUSDT, ETHUSDT
⏱️  Таймфрейм: 15 минут
💰 Начальный баланс: $10000.00

📦 ШАГ 1: Загрузка исторических данных...
✅ Загружено данных для 2 символов

🎯 ШАГ 3: Симуляция торговли...
📈 Прогресс: 100%

📊 РЕЗУЛЬТАТЫ БЭКТЕСТА
💰 Начальный баланс: $10000.00
💰 Финальный баланс: $XXXX.XX
📊 ROI: X.XX%
```

**✅ Успех если:**
- Нет ошибок
- ROI рассчитан
- Показаны метрики

---

### Тест 2: Загрузка данных ⏱️ ~2-3 минуты

**Что проверяет:**
- Загрузка с Bybit API
- Кеширование в PostgreSQL
- Повторная загрузка из кеша (быстрее)

**Запуск:**
```bash
python src/test_data_loader.py
```

**Что должно быть:**
```
📦 Загрузка данных для BTCUSDT...
⏱️  Загрузка с API: ~30 секунд
✅ Сохранено в кеш

📦 Повторная загрузка (из кеша)...
⏱️  Загрузка из БД: ~1 секунда ⚡
✅ Данные загружены из кеша
```

**✅ Успех если:**
- Первая загрузка медленная (с API)
- Вторая загрузка быстрая (из кеша)
- Данные корректны

---

### Тест 3: Комиссии и Slippage ⏱️ ~2-3 минуты

**Что проверяет:**
- Влияние комиссий на результаты
- Влияние slippage
- Сравнение идеальных vs реальных условий

**Запуск:**
```bash
python src/test_fees_slippage.py
```

**Что должно быть:**
```
🎯 ИДЕАЛЬНЫЕ УСЛОВИЯ (без комиссий):
   ROI: 15.2%
   PnL: +$1,520

🎯 РЕАЛИСТИЧНЫЕ УСЛОВИЯ (с комиссиями):
   ROI: 12.8%
   PnL: +$1,280
   Всего комиссий: $12.45

💡 ВЛИЯНИЕ КОМИССИЙ И SLIPPAGE:
   Снижение ROI: 2.4%
   Снижение PnL: $240.00
```

**✅ Успех если:**
- ROI с комиссиями < ROI без комиссий
- Показаны комиссии
- Разница реалистична (0.1-0.5% за сделку)

---

### Тест 4: Продвинутые метрики ⏱️ ~2-3 минуты

**Что проверяет:**
- Sharpe Ratio
- Sortino Ratio
- Calmar Ratio
- Expectancy
- Profit Factor
- Average Trade Duration

**Запуск:**
```bash
python src/test_advanced_metrics.py
```

**Интерактивный режим:**
1. Сначала показывает объяснение метрик
2. Нажмите **Enter** для запуска бэктеста
3. Детальный анализ результатов
4. Балльная оценка (0-5)

**Что должно быть:**
```
📊 Риск-скорректированные метрики:
   Sharpe Ratio: 1.856 ✅ Очень хорошо
   Sortino Ratio: 2.134
   Calmar Ratio: 1.425

💹 Торговые метрики:
   Profit Factor: 2.15
   Expectancy: $12.45 на сделку
   Средняя длительность: 4.2 часа

🏆 ОБЩИЙ БАЛЛ: 4/5 👍 ХОРОШО
```

**✅ Успех если:**
- Все метрики рассчитаны
- Показаны оценки качества
- Итоговый балл > 2/5

---

## 🌐 Тест 5: Web UI ⏱️ ~5 минут

**Что проверяет:**
- Веб-интерфейс работает
- Графики отображаются
- API endpoints работают

**Запуск:**
```bash
python src/web/app.py
```

**В терминале должно быть:**
```
================================================================================
🌐 ЗАПУСК WEB UI ДЛЯ БЭКТЕСТИНГА
================================================================================

📊 Веб-интерфейс доступен по адресу: http://localhost:5000

 * Running on all addresses (0.0.0.0)
 * Running on http://127.0.0.1:5000
```

**В браузере (http://localhost:5000):**

1. **Проверьте интерфейс:**
   - ✅ Панель управления видна
   - ✅ Поля для ввода есть
   - ✅ Кнопка "Запустить бэктест" есть

2. **Запустите бэктест:**
   - Заполните параметры (или оставьте по умолчанию)
   - Нажмите "Запустить бэктест"
   - Дождитесь результатов (~30-60 сек)

3. **Проверьте результаты:**
   - ✅ Метрики отображаются
   - ✅ График баланса (Equity Curve) виден
   - ✅ График просадки (Drawdown) виден
   - ✅ График PnL Distribution виден

**✅ Успех если:**
- Страница загружается
- Бэктест запускается
- Все 3 графика отображаются
- Метрики показываются

---

## 🔍 Проверка результатов

### Что должно быть в хорошем бэктесте:

**Базовые метрики:**
- ✅ ROI > 0% (прибыльность)
- ✅ Win Rate > 50% (больше выигрышей)
- ✅ Max Drawdown < 20% (приемлемый риск)

**Продвинутые метрики:**
- ✅ Sharpe Ratio > 1.0 (хорошая риск-доходность)
- ✅ Sortino Ratio > Sharpe (защита от просадок)
- ✅ Profit Factor > 1.5 (устойчивость)
- ✅ Expectancy > $0 (прибыльность на сделку)

**Графики:**
- ✅ Equity Curve растет вверх (если прибыльно)
- ✅ Drawdown показывает просадки
- ✅ PnL Distribution - больше зеленых столбцов

---

## 🐛 Решение проблем

### Проблема: "Module 'flask' not found"

```bash
# Решение: Установите Flask
pip install flask

# Или переустановите все зависимости
pip install -r requirements.txt
```

### Проблема: "No data for backtest"

**Причины:**
1. Нет подключения к интернету
2. Неправильные API ключи
3. Bybit API недоступен

**Решение:**
```bash
# Проверьте .env файл
cat .env | grep BYBIT

# Должны быть:
# BYBIT_API_KEY=...
# BYBIT_API_SECRET=...

# Проверьте подключение
python -c "from bybit_client import BybitClient; b = BybitClient(); print(b.get_market_data('BTCUSDT'))"
```

### Проблема: Бэктест работает очень долго

**Нормально для первого запуска:**
- Загрузка данных с API: ~30-60 секунд
- Симуляция торговли: ~10-30 секунд

**Повторный запуск будет быстрее:**
- Используется кеш БД: ~5-10 секунд

**Если слишком долго (>5 минут):**
- Уменьшите период (7 дней вместо 30)
- Уменьшите количество символов (1 вместо 2-3)

### Проблема: Web UI не открывается

**Проверьте:**
1. Сервер запущен? (должна быть строка "Running on...")
2. Порт пробрасывается? (панель PORTS в VS Code)
3. Браузер открывает правильный URL? (http://localhost:5000)

**Решение:**
```bash
# Проверьте что порт слушается
netstat -tulpn | grep 5000

# Или перезапустите сервер
# Ctrl+C для остановки
python src/web/app.py
```

### Проблема: Графики не отображаются

**Причины:**
1. Нет данных (total_trades = 0)
2. JavaScript ошибки
3. Chart.js не загрузился

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки
3. Убедитесь что бэктест завершился успешно
4. Проверьте что есть закрытые сделки

---

## ✅ Чек-лист тестирования

Пройдитесь по списку:

- [ ] Комплексный тест запустился без ошибок
- [ ] Базовый бэктест показал результаты
- [ ] Загрузка данных работает (кеш тоже)
- [ ] Комиссии влияют на результаты
- [ ] Все продвинутые метрики рассчитаны
- [ ] Web UI открывается в браузере
- [ ] Бэктест запускается через Web UI
- [ ] Все 3 графика отображаются
- [ ] Метрики показываются корректно

---

## 📊 Интерпретация результатов

### Отличная стратегия:
```
ROI: > 15%
Sharpe: > 2.0
Max DD: < 15%
Win Rate: > 60%
Profit Factor: > 2.0
```
**Вывод:** Стратегия отличная! Можно тестировать на testnet.

### Хорошая стратегия:
```
ROI: 5-15%
Sharpe: 1.0-2.0
Max DD: 15-25%
Win Rate: 50-60%
Profit Factor: 1.5-2.0
```
**Вывод:** Стратегия хорошая, но можно улучшить параметры.

### Приемлемая стратегия:
```
ROI: 0-5%
Sharpe: 0.5-1.0
Max DD: 20-30%
Win Rate: 45-55%
Profit Factor: 1.0-1.5
```
**Вывод:** Стратегия работает, но требует оптимизации.

### Плохая стратегия:
```
ROI: < 0%
Sharpe: < 0.5
Max DD: > 30%
Win Rate: < 45%
Profit Factor: < 1.0
```
**Вывод:** Стратегия убыточна. Нужна переработка.

---

## 🎯 Следующие шаги после тестирования

После успешного тестирования:

1. **Если результаты хорошие:**
   - Оптимизируйте параметры
   - Протестируйте на разных периодах
   - Переходите к Спринту 5 (Risk Management)

2. **Если результаты плохие:**
   - Анализируйте почему
   - Меняйте параметры стратегии
   - Рассмотрите Спринт 6 (ML)

3. **Если всё работает:**
   - Используйте для оптимизации
   - Сравнивайте разные настройки
   - Документируйте лучшие результаты

---

**Удачи в тестировании! 🚀**

