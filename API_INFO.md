# 📡 API для получения данных о цене

## Используемое API

### Bybit API (Unified Trading API)

Система использует **Bybit Unified Trading API** для получения данных о ценах и исторических свечах.

#### Библиотека
- **pybit** - официальная Python библиотека для Bybit API
- Версия: Unified Trading API v5

#### Основные методы

1. **`get_historical_klines()`** - получение исторических свечей
   - Endpoint: `GET /v5/market/kline`
   - Параметры:
     - `category`: "linear" (для фьючерсов)
     - `symbol`: торговая пара (например, "BTCUSDT")
     - `interval`: таймфрейм ('1', '5', '15', '30', '60', '240', 'D', 'W')
     - `start`: начальная временная метка (миллисекунды)
     - `end`: конечная временная метка (миллисекунды)
     - `limit`: количество свечей (максимум 1000)
   
   - Формат ответа:
     ```json
     {
       "result": {
         "list": [
           [startTime, openPrice, highPrice, lowPrice, closePrice, volume, turnover]
         ]
       }
     }
     ```

2. **`get_historical_klines_range()`** - загрузка данных за период с пагинацией
   - Автоматически разбивает большие периоды на батчи по 1000 свечей
   - Обрабатывает обратный порядок данных от Bybit (от новых к старым)

3. **`get_market_data()`** - получение текущих рыночных данных
   - Endpoint: `GET /v5/market/tickers` и `GET /v5/market/kline`
   - Возвращает:
     - Текущую цену (`lastPrice`)
     - Изменение цены за 24ч (`price24hPcnt`)
     - Объем за 24ч (`volume24h`)
     - Исторические цены (последние 100 свечей)

#### Кеширование данных

Данные кешируются в базе данных в таблице `historical_klines`:
- Ускоряет повторные запросы
- Уменьшает нагрузку на API
- Позволяет работать офлайн с уже загруженными данными

#### Ограничения API

- **Rate Limit**: Зависит от типа аккаунта Bybit
- **Максимум свечей за запрос**: 1000
- **Требуется API ключ**: Для некоторых endpoints (не требуется для публичных данных)

#### Конфигурация

Настройки API находятся в `.env`:
```env
BYBIT_API_KEY=your_api_key
BYBIT_API_SECRET=your_api_secret
BYBIT_TESTNET=false  # true для тестовой сети
```

#### Использование в коде

```python
from bybit_client import BybitClient

# Создание клиента
bybit = BybitClient()

# Получение исторических данных
klines = bybit.get_historical_klines(
    symbol="BTCUSDT",
    interval="15",
    start_time=start_timestamp,
    end_time=end_timestamp,
    limit=1000
)

# Получение текущих данных
market_data = bybit.get_market_data("BTCUSDT")
current_price = market_data['price']
```

#### Документация Bybit API

- Официальная документация: https://bybit-exchange.github.io/docs/v5/
- Unified Trading API: https://bybit-exchange.github.io/docs/v5/market/kline


