# 🚀 Быстрый старт

Пошаговая инструкция для запуска торгового бота.

## ⚡ Быстрая установка (5 минут)

### Вариант 1: Docker (рекомендуется)

```bash
# 1. Клонировать репозиторий
git clone <your-repo-url>
cd crypto-trading-bot

# 2. Создать .env файл
cp env.example .env
nano .env  # Заполнить API ключи

# 3. Запустить через Docker Compose
docker-compose up -d

# 4. Проверить логи
docker-compose logs -f trading-bot
```

**Готово! Бот запущен в виртуальном режиме.**

### Вариант 2: Локальная установка

```bash
# 1. Клонировать репозиторий
git clone <your-repo-url>
cd crypto-trading-bot

# 2. Создать виртуальное окружение
python3.11 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Создать .env файл
cp env.example .env
nano .env  # Заполнить API ключи

# 5. Запустить бота
python src/main.py
```

## 🔑 Получение API ключей

### 1. Bybit API Keys

1. Зарегистрируйтесь на [Bybit](https://www.bybit.com/)
2. Перейдите в **Account → API Management**
3. Создайте **Testnet API Key** для безопасного тестирования
4. Скопируйте **API Key** и **Secret**
5. **Важно:** Используйте только Testnet для начала!

**Разрешения (Permissions):**
- ✅ Read/Write (для виртуальной торговли достаточно Read)
- ✅ Derivatives
- ❌ Transfer (не нужно)
- ❌ Withdraw (не нужно)

### 2. DeepSeek API Key

1. Зарегистрируйтесь на [DeepSeek Platform](https://platform.deepseek.com/)
2. Перейдите в **API Keys**
3. Создайте новый ключ
4. Скопируйте ключ (он показывается только один раз!)

**Тарификация:**
- Первые запросы обычно бесплатны
- Проверьте текущие цены на сайте

### 3. Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (имя бота, username)
4. Скопируйте полученный **Bot Token**

**Получение Chat ID:**
1. Отправьте сообщение вашему боту (любое)
2. Перейдите в браузере: `https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates`
3. Найдите `"chat":{"id":XXXXXXX}` - это ваш Chat ID

## ⚙️ Конфигурация .env

Минимальная конфигурация для запуска:

```env
# Bybit API (ОБЯЗАТЕЛЬНО)
BYBIT_API_KEY=your_api_key_here
BYBIT_API_SECRET=your_secret_here
BYBIT_TESTNET=True  # ВАЖНО: True для безопасного тестирования!

# DeepSeek AI (ОБЯЗАТЕЛЬНО)
DEEPSEEK_API_KEY=your_deepseek_key_here

# Telegram (ОБЯЗАТЕЛЬНО)
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# База данных (ОПЦИОНАЛЬНО - по умолчанию SQLite)
DATABASE_URL=
```

**Полный пример в файле `env.example`**

## ✅ Проверка установки

После запуска бота выполните:

### 1. Проверка через логи

```bash
# Docker
docker-compose logs trading-bot | grep "✅"

# Локально
tail -f logs/trading_bot.log | grep "✅"
```

**Должны увидеть:**
```
✅ База данных инициализирована
✅ Настройки виртуального бота загружены
✅ Виртуальный бот запущен
```

### 2. Проверка через Telegram

Откройте бота в Telegram и отправьте:

```
/start
```

**Должны получить:** Приветственное сообщение с инструкциями.

```
/status
```

**Должны получить:** Текущий статус бота и баланс.

### 3. Проверка базы данных

```bash
# Если используете SQLite
sqlite3 data/trading_bot.db "SELECT COUNT(*) FROM virtual_positions;"

# Если используете PostgreSQL
docker exec -it trading-postgres psql -U trader -d trading_bot -c "SELECT COUNT(*) FROM virtual_positions;"
```

## 🎮 Основные команды Telegram

После успешного запуска используйте эти команды:

### Базовые команды

```
/start          - Запуск бота и получение инструкций
/help           - Список всех доступных команд
/status         - Текущий статус и баланс
/balance        - Детальная информация о балансе
```

### Позиции и статистика

```
/positions      - Список открытых позиций
/history        - История закрытых позиций
/stats          - Детальная статистика торговли
/stats 7        - Статистика за последние 7 дней
```

### Настройки

```
/settings                       - Посмотреть все настройки
/set min_confidence 0.75        - Установить минимальную уверенность
/set leverage 10                - Установить леверидж
/set stop_loss_percent 3.0      - Установить stop-loss
```

### Управление

```
/pause          - Приостановить торговлю
/resume         - Возобновить торговлю
/reload         - Перезагрузить настройки из БД
```

## 🧪 Первый тест

### Сценарий 1: Просмотр статистики

```
1. /status          # Проверяем статус
2. /balance         # Смотрим баланс
3. /settings        # Изучаем настройки
```

### Сценарий 2: Изменение настроек

```
1. /set min_confidence 0.60     # Понижаем порог для тестов
2. /set leverage 3              # Устанавливаем безопасный леверидж
3. /settings                    # Проверяем изменения
```

### Сценарий 3: Мониторинг торговли

```
1. Подождите 15 минут (интервал торговли)
2. /positions        # Смотрим открытые позиции
3. /stats            # Проверяем статистику
```

## 🐛 Решение проблем

### Бот не запускается

**Проблема:** `Database connection error`

**Решение:**
```bash
# Проверьте DATABASE_URL в .env
# Для SQLite убедитесь, что папка data/ существует
mkdir -p data/

# Для PostgreSQL проверьте, что контейнер запущен
docker-compose ps
```

---

**Проблема:** `Bybit API error: Invalid API key`

**Решение:**
- Проверьте правильность BYBIT_API_KEY и BYBIT_API_SECRET
- Убедитесь, что используете ключи от Testnet, если BYBIT_TESTNET=True
- Проверьте разрешения API ключа

---

**Проблема:** `Telegram bot не отвечает`

**Решение:**
```bash
# Проверьте токен бота
curl https://api.telegram.org/bot<YOUR_TOKEN>/getMe

# Если ответ "Unauthorized" - токен неправильный
# Создайте нового бота через @BotFather
```

---

**Проблема:** `DeepSeek API error: Rate limit exceeded`

**Решение:**
- Увеличьте TRADING_INTERVAL_MINUTES в настройках
- Проверьте лимиты на DeepSeek Platform
- Добавьте баланс на аккаунт если необходимо

### Логи показывают ошибки

```bash
# Просмотр последних ошибок
docker-compose logs trading-bot | grep "ERROR"

# Детальные логи с временем
docker-compose logs -f --tail=100 trading-bot
```

**Частые ошибки:**

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Module not found` | Неправильные импорты | `pip install -r requirements.txt` |
| `Permission denied` | Права доступа | `chmod 755 src/main.py` |
| `Port already in use` | Порт занят | Измените порт в docker-compose.yml |
| `Out of memory` | Мало RAM | Уменьшите количество символов |

### База данных не создается

```bash
# SQLite
rm data/trading_bot.db  # Удалить старую БД
python src/main.py      # Создаст новую

# PostgreSQL
docker-compose down -v  # Удалить volumes
docker-compose up -d    # Пересоздать
```

## 📊 Мониторинг работы

### Через Docker

```bash
# Статус контейнеров
docker-compose ps

# Использование ресурсов
docker stats trading-bot

# Логи в реальном времени
docker-compose logs -f trading-bot
```

### Через файлы

```bash
# Логи
tail -f logs/trading_bot.log

# База данных (если SQLite)
sqlite3 data/trading_bot.db

# Проверка позиций
sqlite3 data/trading_bot.db "SELECT * FROM virtual_positions WHERE status='open';"
```

## 🎯 Следующие шаги

После успешного запуска:

1. ✅ **Изучите документацию**
   - [README.md](README.md) - общее описание
   - [ARCHITECTURE.md](ARCHITECTURE.md) - архитектура
   - [TASKS.md](TASKS.md) - план развития

2. ✅ **Настройте под себя**
   - Измените торговые символы
   - Настройте риск-менеджмент
   - Выберите оптимальный интервал

3. ✅ **Мониторьте результаты**
   - Проверяйте статистику каждый день
   - Анализируйте успешность сделок
   - Корректируйте настройки

4. ✅ **Когда освоитесь**
   - Запустите бэктестинг (когда будет готов)
   - Попробуйте разные стратегии
   - Рассмотрите переход на реальную торговлю (с осторожностью!)

## ⚠️ Важные напоминания

- 🔴 **Всегда начинайте с Testnet!**
- 🔴 **Используйте только виртуальную торговлю для обучения**
- 🔴 **Не инвестируйте средства, которые не можете потерять**
- 🔴 **Торговля криптовалютами связана с высоким риском**
- 🔴 **Автор не несет ответственности за финансовые потери**

## 💬 Нужна помощь?

- 📖 [README.md](README.md) - общая документация
- 🐛 [GitHub Issues](../../issues) - сообщить о проблеме
- 💡 [CONTRIBUTING.md](CONTRIBUTING.md) - как помочь проекту
- 📧 Напишите в Telegram (если есть группа поддержки)

---

**Приятной торговли! 🚀**

