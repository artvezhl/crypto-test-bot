# 🧪 Руководство по тестированию

## ⚠️ Важно: Версия Python

В этом проекте используется **Python 3.11**. Зависимости установлены только для этой версии.

## 🚀 Запуск тестов

### Комплексный тест всех компонентов Спринта 4:

```bash
python3.11 test_all_sprint4.py
```

**НЕ используйте** `python` (это может быть Python 3.13, где зависимости не установлены).

### Отдельные тесты:

```bash
# Быстрый тест бэктестинга
python3.11 src/test_quick_backtest.py

# Тест таблицы сделок
python3.11 src/test_trades_ui.py

# Тест Web UI (запустите сервер отдельно)
python3.11 src/web/app.py
# Затем откройте http://localhost:5000
```

## 🔍 Проверка зависимостей

Если видите ошибку "НЕ УСТАНОВЛЕН", проверьте:

```bash
# Проверка версии Python
python3.11 --version

# Проверка установленных пакетов
python3.11 -m pip list | grep -E "flask|numpy|pandas|pybit"

# Установка зависимостей (если нужно)
python3.11 -m pip install -r requirements.txt
```

## 📝 Структура тестов

- `test_all_sprint4.py` - Комплексный тест всех компонентов
- `src/test_quick_backtest.py` - Быстрый тест бэктестинга
- `src/test_trades_ui.py` - Тест таблицы сделок в Web UI
- `src/test_advanced_metrics.py` - Тест продвинутых метрик
- `src/test_data_loader.py` - Тест загрузчика данных

## 🌐 Web UI тестирование

1. Запустите сервер:
   ```bash
   python3.11 src/web/app.py
   ```

2. Откройте в браузере:
   ```
   http://localhost:5000
   ```

3. Запустите бэктест через интерфейс

4. Проверьте:
   - ✅ Прогресс-бар работает
   - ✅ Графики отображаются
   - ✅ Таблица сделок показывается
   - ✅ Экспорт CSV/JSON работает

## ⚡ Быстрая проверка

Если нужно быстро проверить что все работает:

```bash
# 1. Проверка зависимостей
python3.11 -c "import flask, numpy, pandas, pybit; print('✅ OK')"

# 2. Быстрый бэктест
python3.11 src/test_quick_backtest.py

# 3. Web UI
python3.11 src/web/app.py
# Откройте http://localhost:5000
```

## 🐛 Решение проблем

### Проблема: "НЕ УСТАНОВЛЕН" при запуске с `python`

**Решение:** Используйте `python3.11` вместо `python`:

```bash
# ❌ Неправильно
python test_all_sprint4.py

# ✅ Правильно
python3.11 test_all_sprint4.py
```

### Проблема: Зависимости не найдены даже с python3.11

**Решение:** Переустановите зависимости:

```bash
python3.11 -m pip install --upgrade -r requirements.txt
```

### Проблема: Web UI не запускается

**Решение:** Проверьте что порт 5000 свободен:

```bash
# Проверка порта
lsof -i :5000

# Остановка процесса если нужно
pkill -f "python3.11 src/web/app.py"
```


