# 🐳 Руководство по работе в Dev Container

> Инструкции для запуска всех компонентов Спринта 4 в dev контейнере

---

## 🚀 Первоначальная настройка

### Шаг 1: Установите зависимости

Контейнер автоматически устанавливает зависимости при создании, но если нужно обновить:

```bash
# В терминале контейнера
pip install -r requirements.txt
```

Проверьте что Flask установлен:

```bash
python -c "import flask; print(flask.__version__)"
# Должно вывести: 3.0.0
```

---

## 🧪 Запуск тестов в контейнере

Все тесты запускаются точно так же, как и локально:

### Тест 1: Базовый бэктест

```bash
cd /app
python src/test_backtest.py
```

**Ожидаемое время:** ~30-60 секунд при первом запуске

**Что увидите:**
- Загрузка данных за 7 дней
- Симуляция торговли
- Отчет с метриками

---

### Тест 2: Загрузка данных

```bash
python src/test_data_loader.py
```

**Проверяет:**
- Загрузку с Bybit API
- Кеширование в PostgreSQL
- Повторную загрузку из кеша (быстрее)

---

### Тест 3: Комиссии и Slippage

```bash
python src/test_fees_slippage.py
```

**Сравнивает:**
- Бэктест без комиссий
- Бэктест с комиссиями
- Показывает реальное влияние

---

### Тест 4: Продвинутые метрики

```bash
python src/test_advanced_metrics.py
```

**Интерактивный режим:**
1. Показывает объяснение метрик
2. Нажмите Enter для запуска
3. Детальный анализ с оценкой

---

## 🌐 Запуск Web UI в контейнере

### Вариант 1: Простой запуск (без внешнего доступа)

```bash
cd /app
python src/web/app.py
```

**Доступ:**
- Внутри контейнера: http://localhost:5000
- **НЕ доступен с хоста** (нужен проброс портов)

---

### Вариант 2: С доступом с хоста (РЕКОМЕНДУЕТСЯ)

Нужно пробросить порт 5000. Два способа:

#### Способ A: Через devcontainer.json (постоянно)

Добавьте в `.devcontainer/devcontainer.json`:

```json
{
  ...
  "forwardPorts": [5000],
  "portsAttributes": {
    "5000": {
      "label": "Web UI",
      "onAutoForward": "notify"
    }
  }
  ...
}
```

Затем **пересоздайте контейнер**:
- Ctrl+Shift+P → "Dev Containers: Rebuild Container"

#### Способ B: Через VS Code UI (временно)

1. Запустите `python src/web/app.py`
2. VS Code покажет уведомление о порте 5000
3. Нажмите "Open in Browser"

Или вручную:
1. Откройте панель "PORTS" в VS Code (снизу)
2. Нажмите "Forward a Port"
3. Введите `5000`
4. Откройте http://localhost:5000 в браузере на хосте

---

### Вариант 3: Запуск на всех интерфейсах

Модифицируйте `src/web/app.py` (последняя строка):

```python
# Было:
app.run(debug=True, host='0.0.0.0', port=5000)

# Оставьте как есть, но убедитесь что host='0.0.0.0'
```

`0.0.0.0` означает что сервер слушает на всех интерфейсах.

---

## 📊 Проверка PostgreSQL в контейнере

Ваш контейнер уже настроен с PostgreSQL. Проверьте подключение:

```bash
# Подключение к PostgreSQL
psql -h localhost -U trader -d trading_bot

# Проверка таблиц
\dt

# Проверка кеша исторических данных
SELECT COUNT(*), symbol, interval 
FROM historical_klines 
GROUP BY symbol, interval;

# Проверка виртуальных позиций
SELECT COUNT(*) as total, status 
FROM virtual_positions 
GROUP BY status;

# Выход
\q
```

---

## 🔍 Отладка в контейнере

### Проверка логов

```bash
# Логи бота
tail -f logs/trading_bot.log

# Если логи пустые или нет файла
ls -la logs/
```

### Проверка переменных окружения

```bash
# Проверка .env файла
cat .env | grep BYBIT

# Должны быть установлены:
# BYBIT_API_KEY=...
# BYBIT_API_SECRET=...
```

### Проверка портов

```bash
# Проверка что Web UI запущен
netstat -tulpn | grep 5000

# Или через Python
python -c "import socket; s=socket.socket(); s.bind(('0.0.0.0', 5000)); print('Port 5000 available')"
```

---

## 💡 Полезные команды для контейнера

### Установка дополнительных пакетов

```bash
# В контейнере
pip install имя_пакета

# Сохранить в requirements.txt
pip freeze > requirements.txt
```

### Проверка Python окружения

```bash
# Версия Python
python --version
# Должна быть 3.11

# Установленные пакеты
pip list

# Путь к Python
which python
# Должен быть /usr/local/bin/python
```

### Очистка кеша

```bash
# Очистка __pycache__
find /app -type d -name __pycache__ -exec rm -rf {} +

# Очистка .pyc файлов
find /app -name "*.pyc" -delete
```

---

## 🚨 Типичные проблемы в контейнере

### Проблема: "Module 'flask' not found"

```bash
# Решение: Переустановите зависимости
pip install -r requirements.txt

# Проверка
python -c "import flask"
```

### Проблема: Web UI не открывается в браузере

**Причина:** Порт не пробрасывается

**Решение:**
1. Откройте панель PORTS в VS Code (снизу)
2. Нажмите "+" и введите `5000`
3. Или добавьте в devcontainer.json (см. выше)

### Проблема: Нет подключения к PostgreSQL

```bash
# Проверьте что PostgreSQL запущен
docker ps | grep postgres

# Проверьте настройки в .env
cat .env | grep DB_

# Должны быть:
# DB_HOST=localhost (или имя сервиса из docker-compose)
# DB_PORT=5432
# DB_NAME=trading_bot
# DB_USER=trader
# DB_PASSWORD=password
```

### Проблема: Ошибка "Permission denied"

```bash
# Проверьте права на директории
ls -la /app/logs
ls -la /app/data

# Если нужно, создайте директории
mkdir -p /app/logs /app/data /app/backups
```

---

## 📝 Рабочий процесс в контейнере

### Ежедневная работа

```bash
# 1. Откройте проект в VS Code
# VS Code автоматически откроет контейнер

# 2. Откройте терминал (уже внутри контейнера)
cd /app

# 3. Запустите нужный тест
python src/test_backtest.py

# 4. Или запустите Web UI
python src/web/app.py
# Откройте http://localhost:5000 в браузере
```

### Перезапуск контейнера

```bash
# В VS Code:
# Ctrl+Shift+P → "Dev Containers: Rebuild Container"

# Или через Docker
docker-compose -f .devcontainer/docker-compose.dev.yml restart
```

---

## 🎯 Быстрый чек-лист

Перед запуском бэктестов проверьте:

- [ ] Контейнер запущен и активен
- [ ] `pip list | grep flask` показывает Flask 3.0.0
- [ ] `.env` файл содержит API ключи Bybit
- [ ] PostgreSQL доступен (можно подключиться)
- [ ] Директории `logs/`, `data/` существуют
- [ ] Для Web UI: порт 5000 пробрасывается

---

## 🌐 Доступ к Web UI - Полная инструкция

### Шаг 1: Пробросьте порт 5000

**Автоматический способ (рекомендуется):**

Добавьте в `.devcontainer/devcontainer.json`:

```json
{
  "name": "Crypto Trading Bot",
  "dockerComposeFile": "docker-compose.dev.yml",
  "service": "trading-bot",
  "workspaceFolder": "/app",
  
  "forwardPorts": [5000],
  
  "portsAttributes": {
    "5000": {
      "label": "Backtesting Web UI",
      "onAutoForward": "notify"
    }
  },
  
  ... остальное без изменений ...
}
```

### Шаг 2: Пересоздайте контейнер

```
Ctrl+Shift+P → "Dev Containers: Rebuild Container"
```

### Шаг 3: Запустите Web UI

```bash
cd /app
python src/web/app.py
```

### Шаг 4: Откройте в браузере

VS Code покажет уведомление "Open in Browser" - нажмите его.

Или откройте вручную:
```
http://localhost:5000
```

---

## 🎨 Визуальное подтверждение работы

### В терминале контейнера должно быть:

```
================================================================================
🌐 ЗАПУСК WEB UI ДЛЯ БЭКТЕСТИНГА
================================================================================

📊 Веб-интерфейс доступен по адресу: http://localhost:5000

Возможности:
  ✅ Запуск бэктестов через веб-интерфейс
  ✅ Интерактивные графики результатов
  ✅ Детальная статистика метрик

Для остановки нажмите Ctrl+C
================================================================================

 * Serving Flask app 'app'
 * Debug mode: on
WARNING: This is a development server. Do not use it in a production deployment.
 * Running on all addresses (0.0.0.0)
 * Running on http://127.0.0.1:5000
 * Running on http://172.17.0.2:5000
```

### В панели PORTS (VS Code) должно быть:

```
PORT    LABEL               LOCAL ADDRESS
5000    Backtesting Web UI  http://localhost:5000
```

---

## 🔧 Дополнительная настройка для docker-compose

Если хотите добавить порты в docker-compose напрямую:

Отредактируйте `.devcontainer/docker-compose.dev.yml`:

```yaml
services:
  trading-bot:
    ...
    ports:
      - "5000:5000"  # Web UI
    ...
```

---

## ✅ Готово!

Теперь вы можете:
- ✅ Запускать все тесты внутри контейнера
- ✅ Использовать Web UI с доступом с хоста
- ✅ Работать с PostgreSQL из контейнера
- ✅ Отлаживать код в изолированной среде

**Вся инфраструктура уже настроена в вашем dev контейнере!** 🚀

---

## 📞 Помощь

Если что-то не работает:
1. Проверьте чек-лист выше
2. Посмотрите логи: `docker logs <container-id>`
3. Пересоздайте контейнер: "Rebuild Container"
4. Проверьте `.env` файл

---

**Создано:** 2025-12-06  
**Для:** Dev Container Setup  
**Версия:** 1.0


