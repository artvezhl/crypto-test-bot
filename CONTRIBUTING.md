# 🤝 Руководство по внесению вклада

Спасибо за интерес к улучшению проекта! Мы рады любому вкладу.

## 📋 Содержание

- [Как начать](#как-начать)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кода](#стандарты-кода)
- [Commit сообщения](#commit-сообщения)
- [Pull Request процесс](#pull-request-процесс)
- [Отчеты об ошибках](#отчеты-об-ошибках)

## 🚀 Как начать

### 1. Fork репозиторий

Нажмите кнопку "Fork" в правом верхнем углу страницы репозитория.

### 2. Клонируйте свой fork

```bash
git clone https://github.com/YOUR_USERNAME/crypto-trading-bot.git
cd crypto-trading-bot
```

### 3. Настройте upstream

```bash
git remote add upstream https://github.com/ORIGINAL_OWNER/crypto-trading-bot.git
```

### 4. Создайте виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

pip install -r requirements.txt
pip install -r requirements-dev.txt  # dev зависимости
```

### 5. Настройте pre-commit hooks

```bash
pip install pre-commit
pre-commit install
```

## 🔄 Процесс разработки

### 1. Синхронизация с upstream

Перед началом работы всегда синхронизируйтесь с основным репозиторием:

```bash
git checkout main
git fetch upstream
git merge upstream/main
git push origin main
```

### 2. Создайте feature branch

```bash
# Для новой функции
git checkout -b feature/название-функции

# Для исправления бага
git checkout -b fix/описание-бага

# Для рефакторинга
git checkout -b refactor/описание
```

### 3. Работайте над задачей

- Следуйте плану из [TASKS.md](TASKS.md)
- Ссылайтесь на номер задачи в commit сообщениях
- Пишите тесты для нового кода
- Обновляйте документацию при необходимости

### 4. Запустите тесты

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=src --cov-report=html

# Быстрые тесты
pytest -m "not slow"

# Конкретный файл
pytest tests/test_database.py
```

### 5. Проверьте код линтерами

```bash
# Flake8
flake8 src/ tests/

# Black (автоформатирование)
black src/ tests/

# isort (сортировка импортов)
isort src/ tests/

# mypy (type checking)
mypy src/
```

## 📝 Стандарты кода

### Python Style Guide

Мы следуем [PEP 8](https://pep8.org/) с некоторыми дополнениями:

- **Максимальная длина строки:** 100 символов
- **Кавычки:** одинарные `'` для строк, двойные `"` для docstrings
- **Импорты:** группируются и сортируются isort
- **Type hints:** обязательны для публичных функций

### Примеры хорошего кода

```python
# ✅ Хорошо
def calculate_position_size(
    balance: float,
    risk_percent: float,
    entry_price: float,
    stop_loss: float
) -> float:
    """
    Рассчитывает размер позиции на основе риска.
    
    Args:
        balance: Доступный баланс в USDT
        risk_percent: Процент риска на сделку (0-100)
        entry_price: Цена входа
        stop_loss: Цена stop-loss
        
    Returns:
        Размер позиции в контрактах
        
    Raises:
        ValueError: Если stop_loss >= entry_price для лонга
    """
    if stop_loss >= entry_price:
        raise ValueError('Stop loss должен быть ниже цены входа для лонга')
    
    risk_amount = balance * (risk_percent / 100)
    price_diff = abs(entry_price - stop_loss)
    position_size = risk_amount / price_diff
    
    return position_size


# ❌ Плохо
def calc(b, r, e, s):
    return b * (r / 100) / abs(e - s)  # Что это вообще?
```

### Docstrings

Используйте Google style docstrings:

```python
def get_virtual_positions(self, symbol: str = None) -> List[Dict]:
    """
    Получает список виртуальных позиций.
    
    Args:
        symbol: Символ для фильтрации. Если None, возвращает все позиции.
        
    Returns:
        Список словарей с данными позиций. Пустой список если позиций нет.
        
    Example:
        >>> bot.get_virtual_positions('BTCUSDT')
        [{'id': 1, 'symbol': 'BTCUSDT', 'side': 'BUY', ...}]
    """
    ...
```

### Логирование

```python
# ✅ Хорошо - структурированное логирование
self.logger.info(
    f"Opening position: {symbol} {side} size={size:.4f} @ ${entry_price:.2f}"
)

# ❌ Плохо - неинформативно
self.logger.info("Position opened")
```

## 💬 Commit сообщения

Используем [Conventional Commits](https://www.conventionalcommits.org/):

### Формат

```
<type>(<scope>): <subject>

[optional body]

[optional footer]
```

### Типы

- `feat`: Новая функция
- `fix`: Исправление бага
- `docs`: Изменения в документации
- `style`: Форматирование, отсутствующие точки с запятой и т.д.
- `refactor`: Рефакторинг кода
- `test`: Добавление тестов
- `chore`: Обновление зависимостей, конфигурации и т.д.

### Примеры

```bash
# Хорошо ✅
feat(database): add transaction support for virtual positions
fix(bot): correct position size calculation with leverage
docs(readme): update installation instructions
test(database): add tests for virtual_positions table
refactor(bot): remove unused self.virtual_positions variable

# С номером задачи
feat(database): [Task 2.2] add context manager for transactions
fix(bot): [Task 1.1] remove self.virtual_positions from memory

# С телом
fix(bot): correct stop-loss triggering logic

The stop-loss was checking against wrong price direction for short positions.
Now properly handles both BUY and SELL positions.

Closes #123

# Плохо ❌
Update files
Fixed bug
WIP
asdfasdf
```

## 🔀 Pull Request процесс

### 1. Убедитесь что ваш код готов

- [ ] Все тесты проходят
- [ ] Линтеры не показывают ошибок
- [ ] Добавлены тесты для нового кода
- [ ] Обновлена документация
- [ ] CHANGELOG.md обновлен

### 2. Создайте Pull Request

Заполните шаблон PR:

```markdown
## Описание

Краткое описание изменений и зачем они нужны.

## Тип изменения

- [ ] Bug fix (исправление бага)
- [ ] New feature (новая функция)
- [ ] Breaking change (несовместимые изменения)
- [ ] Documentation update

## Связанные задачи

Closes #123
Relates to #456

## Как протестировано

Опишите тесты, которые вы запустили:

- [ ] Unit tests
- [ ] Integration tests
- [ ] Manual testing

## Checklist

- [ ] Код следует style guidelines
- [ ] Self-review выполнен
- [ ] Комментарии добавлены в сложные места
- [ ] Документация обновлена
- [ ] Нет новых warnings
- [ ] Тесты добавлены и проходят
- [ ] CHANGELOG.md обновлен
```

### 3. Code Review

- Ответьте на комментарии ревьюеров
- Внесите необходимые изменения
- Запросите повторный review

### 4. Merge

После одобрения ваш PR будет смержен в main branch.

## 🐛 Отчеты об ошибках

### Используйте шаблон issue

При создании issue об ошибке включите:

**Описание бага**
Четкое описание проблемы.

**Как воспроизвести**
Шаги для воспроизведения:
1. Запустить '...'
2. Выполнить '...'
3. Увидеть ошибку

**Ожидаемое поведение**
Что должно было произойти.

**Скриншоты**
Если применимо.

**Окружение:**
 - OS: [например, Ubuntu 22.04]
 - Python версия: [например, 3.11.5]
 - Версия проекта: [например, 0.1.0]

**Логи**
```
Вставьте релевантные логи
```

**Дополнительный контекст**
Любая другая информация о проблеме.

## 💡 Предложения улучшений

Для предложения новых функций создайте issue с тегом `enhancement`:

- Опишите предлагаемую функцию
- Объясните зачем она нужна
- Предложите возможную реализацию
- Укажите альтернативы, которые вы рассматривали

## 📚 Полезные ресурсы

- [План задач (TASKS.md)](TASKS.md)
- [История изменений (CHANGELOG.md)](CHANGELOG.md)
- [Python Style Guide (PEP 8)](https://pep8.org/)
- [Google Python Style Guide](https://google.github.io/styleguide/pyguide.html)
- [Conventional Commits](https://www.conventionalcommits.org/)

## ❓ Вопросы?

Если у вас есть вопросы:

1. Проверьте [README.md](README.md)
2. Посмотрите существующие issues
3. Создайте новый issue с тегом `question`
4. Спросите в Telegram группе проекта

---

**Спасибо за ваш вклад! 🎉**

